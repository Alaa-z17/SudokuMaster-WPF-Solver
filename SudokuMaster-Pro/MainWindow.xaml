<Window x:Class="SudokuMaster_Pro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sudoku Master Pro" Height="650" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F9">
    <Window.Resources>
        <Style x:Key="SudokuCell" TargetType="TextBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="MaxLength" Value="1"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#D4E6F1"/>
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Name="txtTimer" Text="00:00" FontSize="20" FontWeight="Bold" Foreground="#E67E22" Margin="0,0,30,0"/>
            <TextBlock Name="txtBestScore" Text="Best: --:--" FontSize="20" FontWeight="Bold" Foreground="#27AE60" Margin="30,0,0,0"/>
        </StackPanel>

        <TextBlock Text="SUDOKU MASTER" 
                   FontSize="32" FontWeight="ExtraBold" Foreground="#2C3E50"
                   HorizontalAlignment="Center" Margin="0,0,0,20" Grid.Row="1"/>

        <Border BorderBrush="#2C3E50" BorderThickness="3" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Name="MainSudokuGrid" Width="450" Height="450">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="3" Margin="0,20,0,0">

            <ComboBox Name="cmbDifficulty" Width="120" Height="30" Margin="10" SelectedIndex="1">
                <ComboBoxItem Content="Easy" Tag="40"/>
                <ComboBoxItem Content="Medium" Tag="45"/>
                <ComboBoxItem Content="Hard" Tag="55"/>
            </ComboBox>

            <Button Name="btnSolve" Content="Solve Puzzle" Click="btnSolve_Click" Width="120" Height="40" Margin="10" Background="#2ECC71" Foreground="White" FontWeight="Bold"/>
            <Button Name="btnClear" Content="Clear Board" Click="btnClear_Click" Width="120" Height="40" Margin="10" Background="#E74C3C" Foreground="White" FontWeight="Bold"/>
            <Button Name="btnGenerate" Content="Generate New" Click="btnGenerate_Click" Width="120" Height="40" Margin="10" Background="#3498DB" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <MediaElement Name="bgMusic" Source="Assets/background_music.mp3" LoadedBehavior="Play" UnloadedBehavior="Stop" Volume="0.5" Visibility="Collapsed" MediaEnded="bgMusic_MediaEnded"/>
    </Grid>
</Window>