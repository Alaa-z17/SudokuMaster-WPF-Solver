<Window x:Class="SudokuMaster_Pro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sudoku Master Pro" Height="650" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6F9">
    <Window.Resources>
        <Style x:Key="SudokuCell" TargetType="TextBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="MaxLength" Value="1"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="Background" Value="#D4E6F1"/>
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SUDOKU MASTER" 
                   FontSize="32" 
                   FontWeight="ExtraBold" 
                   Foreground="#2C3E50"
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

        <Border BorderBrush="#2C3E50" BorderThickness="3" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">

            <Grid Name="MainSudokuGrid" Width="450" Height="450">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2" Margin="0,20,0,0">
            <Button Name="btnSolve" Content="Solve Puzzle" Click="btnSolve_Click" Width="120" Height="40" Margin="10" Background="#2ECC71" Foreground="White" FontWeight="Bold"/>
            <Button Name="btnClear" Content="Clear Board" Click="btnClear_Click" Width="120" Height="40" Margin="10" Background="#E74C3C" Foreground="White" FontWeight="Bold"/>
            <Button Name="btnGenerate" Content="Generate New" Click="btnGenerate_Click" Width="120" Height="40" Margin="10" Background="#3498DB" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>